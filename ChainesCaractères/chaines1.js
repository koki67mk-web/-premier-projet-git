//Écrivez un programme JavaScript qui lit une chaîne de caractères entrée par
//  l'utilisateur et l'affiche à l'écran
const prompt = require("prompt-sync")();
let text = prompt(" entré un text ")
    console.log(text);
