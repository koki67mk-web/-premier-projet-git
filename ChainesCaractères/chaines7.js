//Écrivez un programme JavaScript qui lit une chaîne de caractères et convertit
//  tous les caractères en minuscules. Affichez la chaîne modifiée.
const prompt = require("prompt-sync")();
let text = prompt(" entré un texts ").toUpperCase();
console.log(text);