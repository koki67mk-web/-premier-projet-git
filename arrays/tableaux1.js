//Écrivez un programme JavaScript qui initialise un tableau d'entiers avec des  
// valeurs données et affiche ces valeurs. Par exemple, vous pouvez initialiser
//  un tableau avec les valeurs [1, 2, 3, 4, 5] et afficher chaque valeur sur une nouvelle ligne.
const val = [ 1 , 2, 3, 4, 5 ];
for(let i= 0 ; i < val.length ; i++ ){
console.log(val[i]);
}